model Author {
  id             String       @id @unique @default(cuid())
  nickname       String?
  nicknameIsFake Boolean?
  profileUrl     String?
  // MyCar          MyCar[]
  // MyCarFix       MyCarFix[]
  // MyCarLink      MyCarLink[]
  // MyCarParts     MyCarParts[]
}