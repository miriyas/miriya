'use client';

import styles from './index.module.scss';

const MiddleWareSample = () => {
  return (
    <main className={styles.middlewareExample}>
      <h1>미들웨어 단순 적용 예제 - 이 페이지는 보이지 않아야함</h1>
    </main>
  );
};

export default MiddleWareSample;
